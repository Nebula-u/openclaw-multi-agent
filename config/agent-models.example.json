{
  "$comment": "agent-models.example.json — 复制为 agent-models.json 并按需修改。安装脚本可用 --ModelConfig 指向本文件，为每个 Agent 指定模型。留空表示继承 OpenClaw 默认。模型 ID 必须是当前 OpenClaw 真实可用的 provider/model 引用；本文件的值仅为占位示例，请以 `openclaw config get agents.defaults.models` 或你的可用模型为准。",
  "schema_version": 1,
  "agents": {
    "manager-agent":     { "model": "" },
    "requirement-agent": { "model": "" },
    "architect-agent":   { "model": "" },
    "developer-agent":   { "model": "" },
    "review-agent":      { "model": "" },
    "test-agent":        { "model": "" },
    "release-agent":     { "model": "" }
  }
}
