{
  "$comment": "Add this as models.providers.newapi-responses. Do not add apiKey here: clone the already configured newapi provider or reference the same OpenClaw-managed secret. The dedicated provider preserves newapi/gpt-5.6-luna for ordinary Chat Completions.",
  "baseUrl": "<same-as-newapi-provider>",
  "api": "openai-responses",
  "models": [
    {
      "id": "gpt-5.6-luna",
      "name": "GPT-5.6 Luna (Responses)",
      "api": "openai-responses",
      "reasoning": true,
      "input": ["text"],
      "contextWindow": 200000,
      "maxTokens": 16384,
      "cost": { "input": 0, "output": 0, "cacheRead": 0, "cacheWrite": 0 }
    }
  ]
}
