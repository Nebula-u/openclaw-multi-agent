{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://openclaw-sdlc-multi-agent/contracts/release-decision.schema.json",
  "title": "ReleaseDecision",
  "type": "object",
  "additionalProperties": true,
  "required": ["schema_version", "workflow_id", "task_id", "run_id", "candidate_commit", "verdict", "evaluated_at", "checks", "evidence_refs"],
  "properties": {
    "schema_version": { "type": "integer", "minimum": 1 },
    "workflow_id": { "type": "string", "pattern": "^WF-" },
    "task_id": { "type": "string", "pattern": "^TASK-[A-Za-z0-9][A-Za-z0-9-]*$" },
    "run_id": { "type": "string", "pattern": "^RUN-[A-Za-z0-9][A-Za-z0-9-]*$" },
    "candidate_commit": { "type": "string", "minLength": 1 },
    "verdict": { "type": "string", "enum": ["GO", "NO_GO", "HOLD"] },
    "verdict_meaning": {
      "type": "string",
      "description": "GO 仅表示 READY_TO_DEPLOY，实际部署事实记录在受控 DEPLOY 结果中",
      "default": "GO == READY_TO_DEPLOY (not deployed)"
    },
    "evaluated_at": { "type": "string", "format": "date-time" },
    "commit_matches_review_and_test": { "type": "boolean" },
    "checks": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "status", "evidence_refs"],
        "properties": {
          "name": { "type": "string" },
          "status": { "type": "string", "enum": ["PASS", "FAIL", "HOLD", "UNKNOWN", "NOT_APPLICABLE"] },
          "evidence_refs": { "type": "array", "minItems": 1, "items": { "type": "string" } },
          "notes": { "type": "string" }
        }
      }
    },
    "evidence_refs": { "type": "array", "minItems": 1, "items": { "type": "string" } },
    "known_issues": { "type": "array", "items": { "type": "string" } },
    "rollback_plan_present": { "type": "boolean" },
    "ops_handoff_present": { "type": "boolean" },
    "isolation_mode": { "type": "string", "enum": ["UNSANDBOXED_LOCAL", "SANDBOXED_DOCKER"] }
  }
}
