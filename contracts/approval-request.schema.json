{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://openclaw-sdlc-multi-agent/contracts/approval-request.schema.json",
  "title": "ApprovalRequest",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version", "decision_id", "workflow_id", "task_id", "run_id",
    "trigger", "summary", "options", "recommended_option", "evidence_refs",
    "created_at", "status"
  ],
  "properties": {
    "schema_version": { "type": "integer", "const": 1 },
    "decision_id": { "type": "string", "pattern": "^DEC-[A-Za-z0-9][A-Za-z0-9-]*$" },
    "workflow_id": { "type": "string", "pattern": "^WF-[A-Za-z0-9][A-Za-z0-9-]*$" },
    "task_id": { "type": ["string", "null"], "pattern": "^TASK-[A-Za-z0-9][A-Za-z0-9-]*$" },
    "run_id": { "type": ["string", "null"], "pattern": "^RUN-[A-Za-z0-9][A-Za-z0-9-]*$" },
    "trigger": {
      "type": "string",
      "enum": [
        "REQUIREMENT_AMBIGUITY", "IMPLEMENTATION_TRADEOFF", "PUBLIC_API_BREAKING_CHANGE",
        "IRREVERSIBLE_DATA_OP", "NEEDS_INSTALL_OR_NETWORK", "NEEDS_CREDENTIALS",
        "INPUT_NOT_GIT_REPO", "INPUT_DIRTY_WORKTREE", "CHANGE_APPROVED_REQ_OR_ARCH",
        "THIRDPARTY_LICENSE_UNCLEAR", "SECURITY_RISK_ACCEPTANCE", "TEST_OR_SECURITY_EXCEPTION",
        "RELEASE_HOLD_OVERRIDE", "RELEASE_DEPLOYMENT", "MAX_REWORK_EXCEEDED", "DESTRUCTIVE_OR_CROSS_PROJECT"
      ]
    },
    "summary": { "type": "string", "minLength": 1 },
    "deployment": {
      "type": "object",
      "additionalProperties": false,
      "required": ["project_id", "candidate_commit", "base_url", "url_path", "final_url", "deployment_target"],
      "properties": {
        "project_id": { "type": "string" },
        "candidate_commit": { "type": "string", "pattern": "^[a-f0-9]{40}$" },
        "base_url": { "const": "https://multiagentforge.cloud" },
        "url_path": { "type": "string" },
        "final_url": { "type": "string" },
        "deployment_target": { "type": "string" }
      }
    },
    "options": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["option_id", "description", "impact", "reversibility"],
        "properties": {
          "option_id": { "type": "string", "minLength": 1 },
          "description": { "type": "string", "minLength": 1 },
          "impact": { "type": "string", "minLength": 1 },
          "reversibility": { "type": "string", "enum": ["reversible", "hard_to_reverse", "irreversible", "unknown"] }
        }
      }
    },
    "recommended_option": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["option_id", "rationale"],
      "properties": {
        "option_id": { "type": "string", "minLength": 1 },
        "rationale": { "type": "string", "minLength": 1 }
      }
    },
    "evidence_refs": { "type": "array", "items": { "type": "string" } },
    "created_at": { "type": "string", "format": "date-time" },
    "status": { "type": "string", "enum": ["PENDING", "RESOLVED", "CANCELLED"] }
  }
}
